const config = {
  slug: "jwt-decoder",

  title: "JWT Decoder & Signature Verifier",

  category: "Developer",

  description:
    "Decode JWT headers and payloads and cryptographically verify JWT signatures directly in your browser.",

  keywords: [
    "jwt decoder",
    "jwt verifier",
    "jwt signature verifier",
    "jwt decode",
    "HS256 verifier",
    "RS256 verifier",
    "JSON Web Token decoder",
  ],

  supports: {
    copy: true,
    download: false,
    share: true,
    reset: true,
  },

  seo: {
    title:
      "JWT Decoder & Signature Verifier | ToolVerse",

    description:
      "Free online JWT decoder and signature verifier. Decode JWT tokens and verify HS256, RS256 and other supported signatures directly in your browser.",
  },
};

export default config;