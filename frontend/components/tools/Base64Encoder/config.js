const config = {
  slug: "base64-encoder",

  title: "Base64 Encoder & Decoder",

  category: "Developer",

  description:
    "Encode and decode Base64 text with UTF-8 support directly in your browser.",

  keywords: [
    "base64 encoder",
    "base64 decoder",
    "base64 encode",
    "base64 decode",
    "base64 converter",
    "online base64 encoder",
  ],

  supports: {
    copy: true,
    download: false,
    share: true,
    reset: true,
  },

  seo: {
    title: "Base64 Encoder & Decoder | ToolVerse",

    description:
      "Free online Base64 encoder and decoder with UTF-8 support. Encode and decode text directly in your browser.",
  },
};

export default config;